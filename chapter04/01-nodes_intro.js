/**
 * What is a node?
 * Every item in a dom tree is a node.
 * Each node can have HTML attributes.
 * Nodes can also have content, including other nodes and text.
 * For example, div, p, h1, title, text, comment, spaces, etc.
 */