/**
 * There are 2 main methods we can use to get the parent node
 * 1. parentNode
 * 2. parentElement
 */

// The DOM API allows us to do anything to elements and their content (usually via JavaScript)
// But we first need a way to reach the corresponding DOM node

// document.documentElement - retrieves html code of the page