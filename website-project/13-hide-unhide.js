/**
 * Change Event
 * 
 * 1. The change event is inherited from the HTMLElement interface
 * 2. It is only available on some elements - not all
 * 3. The change event is fired for <input>, <select> and <textarea> elements
 * 4. For our checkbox, a change event is fired every time the checked state changes
 * 5. Although similar to the oninput event, the change event is different
 */